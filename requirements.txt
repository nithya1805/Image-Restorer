# Tested with Python 3.12 (64-bit) on Windows.
# Install with:
#   pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cpu
# (setup.bat does this for you)
flask==3.1.3                 # the web page
waitress==3.0.2              # server for app.py (web page only)
fastapi==0.141.1             # the API service (api.py)
uvicorn==0.53.0              # server for api.py
python-multipart==0.0.32     # file uploads for FastAPI
a2wsgi==1.10.10              # mounts the Flask page under the FastAPI service
onnxruntime==1.30.0
opencv-python==5.0.0.93
numpy==2.5.2
pillow==12.3.0
einops==0.8.2              # needed by the DocRes (Restormer) model
# CPU-only PyTorch (for the blur model), from the extra index above
torch==2.14.0+cu126
torchvision==0.29.0+cu126
